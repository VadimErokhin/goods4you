export enum TextTypes {
  Primary = "primary",
  Secondary = "secondary",
  Info = "info",
}

export enum ButtonTypes {
  Primary = "primary",
  Apply = "apply",
  Reset = "reset",
  Pagination = "pagination",
}
